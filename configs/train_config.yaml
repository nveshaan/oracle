defaults:
  - override hydra/job_logging: disabled
  
data_path: "data"
results_dir: "results"

model: "DiT-L"   # choices: [list(DiT_models.keys())]

epochs: 10
batch_size: 64
seed: 42
num_workers: 4

log_every: 50
ckpt_every: 5000

wandb:
  project: "oracle"
  name: "yfinance-L-auxilliary"
  log: True